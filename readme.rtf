用Android Studio 打开QA根目录即可

测试机：
MIX2
或者配置好下面环境的测试机（若以后有根据坐标获取元素时，则只能用固定机器）

需要保证以下环境：
1、手机安装并登录QQ
	1845162725
	ludacong
2、安装微信并登录：
	644139490
	52francefrance
2、跳过新手引导
3、推送授权
4、设置手机连usb时不休眠
5、App内未登录

目录结构说明：
1、pages包中含各个页面类，如果是全新的页面需要新建类并继承BasePage类
2、tests包中含各测试用例，写case时在此目录下新建java文件，继承BaseTest类
3、运行时只运行tests目录下所有case即可

写用例原则：
1、保证每个用例执行完毕以后都回复执行之前的状态（比如登录状态）
2、保证最后停留的页面，按3次返回键可以退出App（有的页面过深，3次也退不完，case里面要注意）
3、保证case命名以后，ALoginTest永远在第一个，ZLogoutTest永远在最后一个
4、类、方法、变量命名原则皆按照java中的命名原则